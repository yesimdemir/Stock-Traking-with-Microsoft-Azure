@model StokMVC.Models.Entity.TBLMUSTERILER
@{
    ViewBag.Title = "MusteriGetir";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">Müşteri Güncelle</h4>
                </div>
                <div class="card-body">
                    @using (Html.BeginForm("Guncelle", "Musteri", FormMethod.Post))
                    {
                        <div class="mb-3">
                            @Html.LabelFor(m => m.MUSTERIID, new { @class = "form-label" })
                            @Html.TextBoxFor(m => m.MUSTERIID, new { @class = "form-control", @readonly = "readonly" })
                        </div>
                        <div class="mb-3">
                            @Html.LabelFor(m => m.MUSTERIAD, new { @class = "form-label" })
                            @Html.TextBoxFor(m => m.MUSTERIAD, new { @class = "form-control" })
                        </div>
                        <div class="mb-3">
                            @Html.LabelFor(m => m.MUSTERISOYAD, new { @class = "form-label" })
                            @Html.TextBoxFor(m => m.MUSTERISOYAD, new { @class = "form-control" })
                        </div>
                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary">Müşteri Güncelle</button>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
